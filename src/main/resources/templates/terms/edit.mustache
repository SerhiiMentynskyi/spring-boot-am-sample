<!DOCTYPE html>
<html lang="uk">
{{> partials/head}}
<body class="bg-light">
{{> partials/navbar }}
  <div class="container py-4">
    <h2>Редагувати термін</h2>
    <form action="/terms/{{id}}/edit" method="post" class="mt-3">
      <div class="mb-3">
        <label class="form-label">Термін</label>
        <input name="term" type="text" value="{{termDef.term}}" class="form-control" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Визначення</label>
        <textarea name="definition" class="form-control" rows="4" required>{{termDef.definition}}</textarea>
      </div>
      <button type="submit" class="btn btn-primary">Оновити</button>
      <a href="/terms/" class="btn btn-secondary">Назад</a>
    </form>
  </div>
</body>
</html>
